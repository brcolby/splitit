from venmo_api import Client

# access_token = Client.get_access_token(username='andyzhu68@yahoo.ca',
#                                        password='And!zhu68')

access_token = '0ef6a1cba8607c0eadb60631d7c9be6f956d3cfab3f2c30d05276463509a3e0d'

venmo = Client(access_token=access_token)

arnav = venmo.user.get_user_by_username('Arnav-Mittal')
print(arnav.id)

transactions = venmo.user.get_user_transactions(arnav.id)

for transaction in transactions:
    if('Eric' in str(transaction)):
      print(transaction)
      print('')

print('-------------------------------------')

requests = venmo.payment.get_charge_payments()
for request in requests:
    print(request)
    print('')

# venmo.payment.request_money(0.01, 'test request', arnav.id)